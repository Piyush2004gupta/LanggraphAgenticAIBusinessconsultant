apiVersion: v1
kind: Service
metadata:
  name: ai-frontend-service
  namespace: langgraph-env
spec:
  type: ClusterIP
  selector:
    app: ai-frontend
  ports:
    - protocol: TCP
      port: 5173
      targetPort: 5173
